<CameraScreen>:
    name: 'Camera Screen'

    BoxLayout:
        Label:
            text: 'Camera screen'
        Button:
            text: 'Initialize camera'
            on_release: root.initialize_camera()
